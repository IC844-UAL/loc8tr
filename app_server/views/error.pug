extends layout

block content
  .location-minimal
    .minimal-card
      h1.minimal-title= message
      if error && error.status
        p.text-muted.mb-2 Status: #{error.status}
      if error && error.stack
        pre.small.mb-0= error.stack
